---
description: Run a full UI design pass before implementing or redesigning a screen/component.
---

# UI Design Pass Workflow

1. Read local UI/style docs and inspect existing components/tokens.
2. Create a direction brief: purpose, audience, tone, primary action, memorable detail, constraints.
3. Map states: default, loading, empty, error, disabled, success, permission-denied.
4. Design hierarchy and layout first, then visual polish.
5. Implement using existing components/tokens unless a new pattern is justified.
6. Verify mobile and desktop fit, keyboard access, labels, focus, and browser behavior when feasible.
7. Report direction, files changed, proof, and residual design risks.

