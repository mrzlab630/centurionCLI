{
  "name": "claude-legion-kit",
  "version": "0.1.0",
  "private": true,
  "description": "CENTURION Legion plugin, subagents, and guarded CLAUDE_ORDER v1 execution for Claude Code.",
  "type": "module",
  "scripts": {
    "smoke": "node ./scripts/smoke.mjs",
    "install:local": "node ./installer/install.mjs",
    "plugin:validate": "claude plugin validate ./plugin --strict"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
